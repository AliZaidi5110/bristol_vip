# Hero videos

| File | Purpose |
|---|---|
| `hero-web.mp4` | **Production hero** (~4–5 MB, 35s loop) — used by the site |
| `hero-compressed.mp4` | Optional local mid-size encode (gitignored) |
| `IMG_8136.MP4` | Original source (gitignored — too large for GitHub) |

Regenerate the web clip:

```bash
npm run compress-video
```
